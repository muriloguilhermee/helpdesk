# Configuração do Banco de Dados PostgreSQL/Supabase
# Copie este arquivo para .env.local e configure

# Opção 1: Supabase (Recomendado)
# Deixe vazio para usar IndexedDB (modo local)
VITE_SUPABASE_URL=https://seu-projeto.supabase.co
VITE_SUPABASE_ANON_KEY=sua-chave-anon-key-aqui

# O sistema detecta automaticamente:
# - Se VITE_SUPABASE_URL estiver configurado → usa PostgreSQL
# - Se não estiver → usa IndexedDB (local)


